# OpenCode API key — get at https://opencode.ai/auth (or via `opencode` /connect)
OPENCODE_API_KEY=

# GitHub PAT for `gh` inside the sandbox (repo + issues scope)
# Can match your logged-in gh token: gh auth token
GH_TOKEN=
